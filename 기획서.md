# morrow To-do 앱 기획서

## 1. 프로젝트 개요

`morrow`는 오늘 해야 할 일에만 집중할 수 있도록 만든 애플 스타일의 차분한 To-do 앱이다. 많은 기능을 한 화면에 밀어 넣기보다, 해야 할 일과 오늘의 진행률을 한눈에 보여주는 것을 핵심 경험으로 삼는다.

- 프로젝트 유형: Next.js App Router 기반 풀스택 웹앱
- 초기 데이터 저장: 브라우저 `localStorage` (데이터베이스 없음)
- 초기 배포: 하지 않음. 로컬 `localhost:3000`에서 확인
- 제품 톤: 조용함, 여백, 따뜻한 아이보리, 세이지 그린, 낮은 대비
- 대상 사용자: 하루의 우선순위를 가볍게 정리하고 싶은 개인 사용자

## 2. 문제 정의

기존의 To-do 앱은 기능과 정보가 많아 오늘 당장 무엇을 해야 하는지 오히려 흐릴 수 있다. `morrow`는 다음 질문에 답하는 작은 화면을 제공한다.

1. 오늘 해야 할 일은 무엇인가?
2. 지금 몇 개를 끝냈는가?
3. 남은 일을 어떤 순서로 처리하면 좋은가?

## 3. 핵심 기능

### 할 일 관리

- 할 일 추가
- 우선순위 선택: 높음 / 보통 / 낮음
- 완료 및 완료 취소
- 할 일 삭제
- 완료한 일 한 번에 지우기
- 새로고침 후에도 브라우저에 데이터 유지

### 탐색과 정리

- 전체 / 진행 중 / 완료 필터
- 제목 검색
- 사이드바 숫자로 남은 일과 완료한 일 요약
- 오늘의 완료율을 진행률 링과 막대로 표현

### 반응형 경험

- 데스크톱: 사이드바, 할 일 목록, 인사이트 카드의 3열 경험
- 태블릿: 상단 메뉴와 2열 인사이트 카드
- 모바일: 가로 스크롤 메뉴와 한 열 카드 구성
- 키보드 포커스, 버튼 aria-label, 완료 상태 aria-pressed 제공

## 4. 화면 구성

### 사이드바

- 브랜드 로고와 한 줄 설명
- 오늘의 할 일 / 진행 중 / 완료한 일 메뉴
- 오늘의 진행률 카드
- 사용자 프로필 영역

### 메인 영역

- 오늘 날짜와 환영 문구
- 전체 / 완료 / 남음 통계
- 할 일 입력 폼
- 필터 탭과 검색
- 할 일 목록

### 인사이트 영역

- 짧은 응원 문구
- 오늘의 완료율과 이번 주 리듬
- 연속 기록 카드

## 5. 데이터 모델

```ts
type Todo = {
  id: string;
  title: string;
  priority: "high" | "medium" | "low";
  completed: boolean;
  createdAt: string;
};
```

초기 버전에서는 `localStorage` 키 `morrow-todos-v1`에 JSON 배열로 저장한다. 이후 Supabase 연동 시 이 모델을 PostgreSQL 테이블로 옮기고, UI의 상태 관리와 저장 계층을 분리한다.

## 6. 기술 계획

- 프레임워크: Next.js App Router
- UI: React Client Component 1개 + 전역 CSS
- 저장: 초기에는 `localStorage`, 다음 단계에서 Supabase PostgreSQL
- 인증: 초기 버전에서는 사용하지 않음
- 배포: 초기 버전에서는 사용하지 않음
- 검증: `next build`, ESLint, localhost 주요 흐름 확인

## 7. 후속 확장 순서

1. 초기 로컬 버전의 기능 및 접근성 확인
2. Git 커밋으로 안정 버전 저장
3. Supabase 테이블 생성 및 저장 계층 교체
4. Vercel 환경 변수 설정과 배포
5. GitHub 공개 저장소 생성 및 푸시

후속 단계에서 사용할 테이블 이름은 프로젝트명을 앞에 붙여 `morrow_todos`로 한다.
